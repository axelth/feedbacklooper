<!doctype html>
<html>
  <head>
    <meta charset="utf8">
    <script type="text/javascript" src="<%=url('/master-frontend.js')%>"></script>
    <link rel="stylesheet" type="text/css" href="<%=url('/master-frontend.css')%>">
  </head>
  <body onload="glob.setPrefix();<%= defined?(@composition) ? "glob.compositionPageSetup(#{@composition.id},#{session[:usertype] == 'Teacher'});" : '' %>">
    <%= yield %>
  </body>
</html>
